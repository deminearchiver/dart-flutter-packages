name: touch_targets
description: Create overflowing touch targets to make accessible design-sized components.
version: 0.5.0
homepage: https://github.com/deminearchiver/dart-flutter-packages
repository: https://github.com/deminearchiver/dart-flutter-packages/tree/main/packages/touch_targets
issue_tracker: https://github.com/deminearchiver/dart-flutter-packages/issues
topics: [accessibility, touch, hittest]
resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  # SDK dependencies
  flutter:
    sdk: flutter

  # Hosted dependencies
  collection: ^1.19.1

dev_dependencies:
  # SDK dependencies
  flutter_test:
    sdk: flutter

  # Hosted dependencies
  deminearchiver_lints: ^2.1.2

# TODO: should this be removed?
flutter:
